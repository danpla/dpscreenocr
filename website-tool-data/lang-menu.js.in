document.getElementById("lang-menu").onchange = (event) => {
  const select = event.target;

  try {
    localStorage.setItem(
        "@JS_STORAGE_LANG_KEY@", select[select.selectedIndex].lang);
  } catch {
  }

  window.location = select.value;
};
